# Example: Using Hugging Face Model instead of local artifacts

# Option 1: Set environment variable
HF_MODEL_REPO=your-username/pm-agent-model

# Option 2: Use in docker-compose.yml
# Add to pm-agent service:
#   environment:
#     - HF_MODEL_REPO=your-username/pm-agent-model

# Option 3: Use in Railway
# Set in Railway dashboard environment variables:
# HF_MODEL_REPO = your-username/pm-agent-model

# For private models, also set:
# HUGGING_FACE_HUB_TOKEN=your_hf_token_here
